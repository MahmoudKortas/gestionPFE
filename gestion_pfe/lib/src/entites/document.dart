class Document{
  final int idDoc;
  final String titre;
  final String description;
  final String proprietaire;
  final DateTime dateDepo;

  Document(this.idDoc, this.titre, this.description, this.proprietaire, this.dateDepo);
  
}